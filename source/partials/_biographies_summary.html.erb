<dl>
  <dt>name</dt>
  <dd><%= bio.name %></dd>
  <dt>slug</dt>
  <dd><%= bio.slug %></dd>
  <dt>biography</dt>
  <dd><%= Kramdown::Document.new(bio.biography).to_html %></dd>
  <dt>photo</dt>
  <dd><%= "<img src=\"#{bio.photo.url}?w=400\" alt=\"#{bio.name}\">" if bio.photo %></dd>
</dl>
